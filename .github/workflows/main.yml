name: Build adbfs for Windows

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install cross-compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64-posix make

      - name: Download WinFsp headers
        run: |
          sudo mkdir -p /usr/local/include/fuse
          sudo wget https://gitee.com/mirrors/winfsp/raw/master/inc/fuse/fuse.h -O /usr/local/include/fuse/fuse.h
          sudo wget https://gitee.com/mirrors/winfsp/raw/master/inc/fuse/fuse_common.h -O /usr/local/include/fuse/fuse_common.h
          sudo wget https://gitee.com/mirrors/winfsp/raw/master/inc/fuse/fuse_opt.h -O /usr/local/include/fuse/fuse_opt.h
          sudo wget https://gitee.com/mirrors/winfsp/raw/master/inc/fuse/winfsp_fuse.h -O /usr/local/include/fuse/winfsp_fuse.h

      - name: Patch source for Windows
        run: |
          sed -i '/#include <execinfo.h>/s/^/\/\/ /' adbfs.cpp
          sed -i '/#include <pwd.h>/s/^/\/\/ /' adbfs.cpp
          sed -i '/#include <grp.h>/s/^/\/\/ /' adbfs.cpp
          sed -i '/#include <sys\/types.h>/s/^/\/\/ /' adbfs.cpp
          sed -i '/#include <unistd.h>/s/^/\/\/ /' adbfs.cpp
          sed -i '/#include <sys\/stat.h>/s/^/\/\/ /' adbfs.cpp

      - name: Compile for Windows
        run: |
          x86_64-w64-mingw32-g++-posix -c -o adbfs.o adbfs.cpp -Wall -I/usr/local/include/fuse -D_FILE_OFFSET_BITS=64 -D__MINGW_USE_VC2005_COMPAT
          x86_64-w64-mingw32-g++-posix -o adbfs.exe adbfs.o -lws2_32

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: adbfs-windows
          path: adbfs.exe
